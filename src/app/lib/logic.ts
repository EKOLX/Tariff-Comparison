import { Product } from "../models/product.model";
import { Calculation } from "../models/calculation.model";

export const calculateCosts = (product: Product, calculation: Calculation) => {
  return -11;
};
